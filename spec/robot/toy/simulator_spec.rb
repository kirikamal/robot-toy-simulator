require "spec_helper"

RSpec.describe Robot::Toy::Simulator do
  it "has a version number" do
    expect(Robot::Toy::Simulator::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
